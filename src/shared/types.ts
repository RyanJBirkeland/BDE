/**
 * Shared types — re-exported from domain-grouped modules for backward compatibility.
 * All imports should use `from './types'` — this file delegates to the split modules.
 */

export * from './types/index'
