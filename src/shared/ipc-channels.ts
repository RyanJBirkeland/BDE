/**
 * Typed IPC channel map — re-exported from domain-grouped modules for backward compatibility.
 * All imports should use `from './ipc-channels'` — this file delegates to the split modules.
 */

export * from './ipc-channels/index'
